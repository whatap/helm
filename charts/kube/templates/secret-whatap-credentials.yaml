apiVersion: v1
kind: Secret
metadata:
  name: whatap-credentials
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  WHATAP_LICENSE: {{ .Values.whatap.license | b64enc }}
  WHATAP_HOST: {{ .Values.whatap.host | b64enc }}
  WHATAP_PORT: {{ .Values.whatap.port | toString | b64enc }}